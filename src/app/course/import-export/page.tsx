import ImportCenter from "./components/ImportCenter";

export const metadata = {
  title: "Import & Export | Course Management",
};

export default function ImportExportPage() {
  return <ImportCenter />;
}
